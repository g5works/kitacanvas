import { CourseType } from "../types/course";

export class Course {

    //if being used correctly, this should never have to be called manually
    constructor(course: CourseType) {
        
    }

}